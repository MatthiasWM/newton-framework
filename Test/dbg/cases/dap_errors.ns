// Errors in a DAP session: a launch request without a program gets an error
// response; an exception in the program stops it ("All Exceptions" is on
// by default), is reported as "stderr" output, and after "continue" ends
// the program with exit code 1.
Print("before");
Print(1 + 'a);
Print("not reached");
